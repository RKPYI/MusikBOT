# Discord Bot Token
# Get yours at: https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# ─── YouTube Authentication ──────────────────────────────────────────────────
# YouTube blocks automated requests. Pick ONE method:
#
# Method 1 (RECOMMENDED — permanent):
#   Install the PO Token plugin + run the Docker server.
#   See README.md for setup instructions.
#
# Method 2: Place a cookies.txt (Netscape format) in the project root.
#   Use a browser extension like "Get cookies.txt LOCALLY" to export.
#   ⚠️  Cookies expire — you'll need to re-export periodically.
#
# Method 3: Auto-extract cookies from a local browser (desktop only).
#   Supported: chrome, firefox, edge, opera, brave, chromium, safari
# COOKIES_FROM_BROWSER=firefox
